# Built with Agents

A public log of projects I've built alongside AI agents — what they are, how I worked with the tools, and what I'd do differently. The "AI workflow" section on each case study is the part actually worth reading.

See [`MVP.md`](./MVP.md) for the original spec.

## Stack

- Astro 5 (static output, Content Collections)
- Tailwind CSS v4 (via `@tailwindcss/vite`)
- TypeScript strict
- MDX for case studies
- React islands only where interactivity is genuinely needed (`VideoEmbed`, `CopyLinkButton`)
- Self-hosted Inter + JetBrains Mono (no third-party font CDN)
- Vercel deploy + Vercel Analytics

## Local dev

Requires Node 20+ and pnpm.

```bash
pnpm install
pnpm dev          # http://localhost:4321
pnpm build        # produces dist/
pnpm preview      # serves dist/ locally
```

## Authoring a project

Each case study is one MDX file under `src/content/projects/<slug>.mdx`. The route is `/projects/<slug>`.

For any repo that follows the project-tracker convention (single-HTML utility, README + `initial-prompt.md`), there's a Claude Code slash command that drafts the MDX from a GitHub URL — see [Auto-generating from a GitHub repo](#auto-generating-from-a-github-repo) below. Otherwise, hand-author following the schema.

### Frontmatter schema

Validated by `src/content.config.ts`. Mirrors the spec in `MVP.md`.

```yaml
title: string
oneLiner: string
status: 'live' | 'archived' | 'wip'
year: number
role: 'solo' | 'lead' | 'contributor'
stack: string[]
aiTools: string[]                   # e.g. 'Claude Code', 'Cursor'
links:                              # omit any field you don't have a real URL for
  github: string                    # optional
  publicUrl: string                 # optional
  video: string                     # optional — Loom or YouTube; renders embed when present
heroImage:                          # optional
  src: ./relative/path/to/image.png
  alt: string
order: number                       # sort key on landing
featured: boolean                   # one project gets the featured slot
```

### Required body sections

The MDX body must include these five `##` headings, in order:

1. The problem
2. **AI workflow** — the section worth reading. Don't skip; don't stub.
3. Architecture
4. Outcomes
5. What I'd do differently

### Conventions

- Don't fabricate links or descriptions. If you don't have a real URL, omit the field — `LinkBar` only renders buttons for present links.
- One project at a time, real prose only. Placeholder text is fine while drafting; ship real content before flipping `status: 'wip'` to `'live'`.
- For video embeds, paste a Loom or YouTube URL into `links.video`. The page lazy-loads the iframe and locks aspect ratio for CLS = 0.

## Auto-generating from a GitHub repo

For repos that follow the project-tracker convention — a single-HTML utility with a `README.md` (human description) and an `initial-prompt.md` (the prompts that built it) — you can draft the MDX from Claude Code with one command.

### Usage

In Claude Code, with this repo open:

```
/new-project https://github.com/luisgrandegg/project-tracker
```

Claude will:

1. Fetch the repo's README, `initial-prompt.md`, `package.json`, `CLAUDE.md`, and any root-level `.html`.
2. Auto-detect what it can: `title`, `oneLiner`, `slug`, `year`, `stack`, `aiTools`, `links.github`, `order`.
3. Draft the five required body sections from the README + prompts.
4. Ask you about the things it can't reliably infer: `status`, `role`, `featured`, `links.publicUrl`, `links.video`, and "What I'd do differently".
5. Show you the full proposed MDX and wait for confirmation.
6. Write the file via the underlying CLI — it will not commit or push.

The slash command lives at `.claude/commands/new-project.md`.

### Underlying CLI

The writer is a deterministic Node script that takes a JSON spec and validates it against the content collection schema before writing. You can call it directly without going through Claude Code:

```bash
pnpm gen:project --from spec.json            # write src/content/projects/<slug>.mdx
pnpm gen:project --from spec.json --dry-run  # print the MDX, don't write
pnpm gen:project --stdin < spec.json         # read spec from stdin
pnpm gen:project --help                       # show JSON spec shape
```

If the spec is invalid (missing required field, bad enum, malformed URL, body section empty), the script exits non-zero with a list of errors and writes nothing.

## Routes

- `/` — landing (hero, featured project, project list)
- `/projects/[slug]` — case study
- `/about`
- `/og/[slug].png` — auto-generated 1200×630 social card
- `/sitemap-index.xml` — generated by `@astrojs/sitemap`

## Project structure

```
scripts/
  gen-project.mjs       JSON-spec → validated MDX writer (used by /new-project)
.claude/
  commands/
    new-project.md      Claude Code slash command for auto-generating entries
src/
  assets/fonts/         self-hosted Inter + JetBrains Mono (woff2 + ttf for OG)
  components/           Astro components + 2 React islands
  content/projects/     MDX case studies
  content.config.ts     collection schema (zod)
  layouts/              BaseLayout, CaseStudyLayout
  pages/
    index.astro
    about.astro
    404.astro
    projects/[slug].astro
    og/[slug].png.ts    satori + resvg renderer
  styles/global.css     Tailwind v4 import + design tokens + prose styles
public/
  fonts/                woff2 served at /fonts/*
  favicon.svg
  robots.txt
astro.config.mjs
```

## Design tokens

Defined as Tailwind v4 `@theme` variables in `src/styles/global.css`. The accent is a deep, restrained green (`#15803d` light / `#4ade80` dark). Dark mode follows `prefers-color-scheme` — there's no toggle in the MVP.

## Deploy (Vercel — recommended)

The default config uses `@astrojs/vercel` and Vercel Analytics.

1. Push the repo to GitHub.
2. In Vercel: **New Project** → import the repo. Vercel auto-detects Astro; build command `pnpm build`, output handled by the adapter.
3. Set the production domain in **Settings → Domains**. Update `site` in `astro.config.mjs` to the canonical URL so absolute OG image URLs resolve.
4. Vercel Analytics is enabled in the adapter config — no extra steps.

## Deploy (Cloudflare Pages — alternative)

Swap the adapter:

```bash
pnpm remove @astrojs/vercel @vercel/analytics
pnpm add @astrojs/cloudflare
```

In `astro.config.mjs`:

```js
import cloudflare from '@astrojs/cloudflare';
// ...
adapter: cloudflare(),
```

In Cloudflare Pages: build command `pnpm build`, output directory `dist/`. For analytics, use Cloudflare Web Analytics (no cookie banner required).

## Known follow-ups before v1 ships

- Fill in real prose for `project-tracker.mdx` (especially the AI workflow section).
- Add at least two more project case studies to satisfy the "three populated" acceptance criterion.
- Add a real Loom/YouTube URL to one project's `links.video` to satisfy the embedded-video acceptance criterion.
- Replace the placeholder `site` URL in `astro.config.mjs` with the production domain.
